//"=>"号左侧是参数类型，右侧返回值类型
var HeAdd = function (x, y) { return x + y; };
var secondHeAdd = function (x, y) { return x + y; };
var thirdHeAdd = function (x, y) { return x + y; };
var number = function (x, y) {
    return x + y;
};
var myAdd = function (x, y) {
    return number(x, y);
};
